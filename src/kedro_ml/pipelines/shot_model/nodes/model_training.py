import pandas as pd
from pycaret.regression import *

def model_training(train_set: pd.DataFrame, test_set: pd.DataFrame) -> tuple:
    """
    Train the model based on the variables passed as parameters.

    Args:
        dev_dataset (pd.DataFrame): The raw development dataset.
        prod_dataset (pd.DataFrame): The raw production dataset.

    Returns:
    """
    target_column = 'shot_made_flag'
    

    
    
    return []
    
    
